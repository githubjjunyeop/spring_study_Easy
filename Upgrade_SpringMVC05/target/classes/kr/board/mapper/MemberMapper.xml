<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="kr.board.mapper.MemberMapper">

	<select id="registerCheck" resultType="kr.board.entity.Member">
		SELECT * FROM mem_tbl where memID=#{memID}
	</select>
	
	<insert id="register" parameterType="kr.board.entity.Member">
	INSERT INTO mem_stbl(memIdx, memID, memPassword, memName, memAge, memGender, memEmail, memProfile) 
			VALUES((SELECT IFNULL(MAX(memIdx)+1, 1) FROM mem_stbl mem), #{memID}, #{memPassword}, #{memName}, #{memAge}, #{memGender}, #{memEmail}, #{memProfile})
	</insert>
	
	<select id="memLogin" parameterType="kr.board.entity.Member" resultType="kr.board.entity.Member">
		SELECT * FROM mem_tbl 
		WHERE memID=#{memID} AND memPassword=#{memPassword}
	</select>
	
	<update id="memUpdate" parameterType="kr.board.entity.Member">
		UPDATE mem_tbl SET memPassword=#{memPassword}, memName=#{memName}, memAge=#{memAge}, memGender=#{memGender},
		memEmail=#{memEmail} 	
		WHERE memID=#{memID}
	</update>
	
	<!-- 수정(회원,권한 join) -->
	<select id="getMember" resultType="kr.board.entity.Member">
		SELECT * FROM mem_tbl, authList WHERE memID=#{memID}
	</select>
	
	<update id="memProfileUpdate" parameterType="kr.board.entity.Member">
		UPDATE mem_tbl SET memProfile=#{memProfile} WHERE memID=#{memID}
	</update>
	
	<insert id="authInsert" parameterType="kr.board.entity.Auth" >
		INSERT INRO mem_auth(memID, auth) VALUES(#{memID}, #{auth})
	</insert>

</mapper>